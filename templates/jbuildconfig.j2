kind: "BuildConfig"
apiVersion: "v1"
metadata:
  name: "app-pipeline"
spec:
  source:
    type: "Git"
    git:
      uri: "{{ git_url }}"
  strategy:
    type: "JenkinsPipeline"
    jenkinsPipelineStrategy:
      jenkinsfilePath: